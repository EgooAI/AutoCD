[project]
name = "autocd"
version = "0.1.0"
description = "A single-file, interactive Git deployment watcher"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"
